# 第一版验收记录

## 2026-09-15 设备多图与归还照片

- 新增/编辑设备支持一次多选或分批追加图片，保留上传顺序，第一张为封面；移除首图后下一张接替。详情页缩略图可切换查看。旧单图资料自动保留。
- 归还界面要求上传一张同时包含设备及当前放置位置的照片，提示故障须备注。后端拒绝缺图、虚构路径及其他预约的照片；已上传照片和归还记录绑定保存，可在记录查询中查看。
- 145 项自动测试通过；覆盖 CloudBase / Supabase PostgreSQL 迁移、多图兼容、必传照片及私有存储的上传、读取、防覆盖权限。当前 CloudBase SDK 的签名地址字段为 `fullSignedURL`，已单独适配。
- 本地浏览器验证一次上传两张、首图封面、详情切换，以及无图禁止提交、上传预览、归还成功和历史照片查看；控制台无错误。修正了大屏下缩略图被主图高度挤出的问题。
- CloudBase 迁移 `20260915000003_equipment_return_photos.sql` 已应用。真实腾讯云测试已验证私有照片上传、签名下载字节一致、公开读取拒绝、无图归还拒绝、带图归还记录以及设备多图顺序。
- 临时测试账号、原生身份、设备、预约、照片、注册申请及临时清理策略已移除，云端计数均为零。TypeScript 与正式 Vite 构建通过，产物检查确保不包含演示账号或前端答案题库。

## 2026-09-15 跨天预约与方格范围选择

- 起止日期和时间分别设置；日历第一次点击选起点，第二次点击选终点（包含末格），第三次清空旧终点并选择新起点。切换日历日期保留选中范围，新增日期选择器用于查看七天以外的日期。
- 连续跨天预约保存为一条申请，累计时长按完整时间区间计算。跨天期间必须持续开放，中间的占用同样阻止申请；审批、续约、取消和准入校验保持有效。
- 138 项测试通过，生产构建通过；覆盖跨天、跨月、跨年、零点边界、中间休息日和占用、三次点击循环和跨天高亮。
- CloudBase 增量迁移 `20260915000002_multi_day_bookings.sql` 已应用。随后在云端只读事务中执行五个时间校验用例，跨年和多日允许、午夜结束不占下一日，中间休息日及夜间关闭拒绝；没有新增真实设备或预约。
- 本地浏览器验证：第一次和第三次点击清空终点；次日第二次点击保留起点；`09/16 23:30—09/17 01:00` 显示 1.5 小时且提交为一条待审批记录。该演示申请已取消。直接选择 `09/16 00:00—09/18 00:00` 显示 48 小时。日期表单布局已目视检查。

日期：2026-09-13。仓库现归属 [HCLab-gz/HCLab-Equipment-Management](https://github.com/HCLab-gz/HCLab-Equipment-Management)。Supabase 项目尚未连接，当前不是多人共享预约系统。

## 2026-09-14 试用反馈修正

- 删除左上角标语和 504 / 505 的功能分区名称，不再推定两间房间各自的用途。
- 准入考试改为随机十题全部答对、100 分才合格。界面、演示服务、条例文案和数据库增量迁移 004 同步更新，条例版本为 2026-09-v2。
- 20 项测试通过，覆盖 80 / 90 分无法取得有效注册资格、100 分可以注册，以及注册入口拒绝非满分凭证。TypeScript 与 Vite 构建通过。
- 本地浏览器已核对新版首页、100 分提示和未核实示例说明，控制台无错误或警告；390 像素窄屏无横向溢出，已恢复默认视口。
- 示例设备全部关联 demo-admin；设备的房间、管理员、项目和具体位置由演示种子数据指定，不是用户文档中已经核实的资产资料。新增顶部说明适用于已经保存在浏览器里的演示数据，没有清空用户试用记录。
- 正式后端尚未连接，迁移 004 未在云端执行；以后部署时须按 DEPLOYMENT.md 完整执行迁移。公开发布仍保持关闭。

## 暂缓公开发布

- 用户要求只下线网站，源码仓库保持公开。
- 工作流已改为推送时只测试和构建；上传及部署仅在手动运行并明确勾选 publish 时执行，默认关闭。[关闭自动发布后的 Actions](https://github.com/HCLab-gz/HCLab-Equipment-Management/actions/runs/34766404873) 已成功，Pages 产物上传步骤和 deploy 作业均为 skipped。
- HCLab-gz 已在 Settings → Pages 取消发布；公网入口 `https://hclab-gz.github.io/HCLab-Equipment-Management/` 已验证返回 HTTP 404，页面标题为“Site not found · GitHub Pages”。
- 下列上线结果是历史验收记录，不代表网站应继续公开。

## 仓库归属调整

- 从 YixFeng 转移至 HCLab-gz，接收方已接受转移。仓库 ID 仍为 `1368397092`，转移前后 main 提交均为 `b8b7180190f628ab4575bd969bd032bda3a92d80`，保留已有提交历史及 Issues 功能（转移时无未关闭 Issue）。
- 本地 origin、README、部署示例及未来账号服务的登录回调地址说明已更新至 HCLab-gz。
- GitHub Pages 使用 `https://hclab-gz.github.io/HCLab-Equipment-Management/`。旧 Pages 地址不应继续作为访问入口；历史验收结果见下文，新地址部署结果以仓库 Actions 为准。

## 发布验证

- [首次发布 Actions](https://github.com/HCLab-gz/HCLab-Equipment-Management/actions/runs/34759088080) 的安装、测试、构建与部署步骤全部成功。
- Pages 公网首页返回 HTTP 200，HTML 标题及项目子目录中的脚本、样式引用正确。
- 本地生产构建预览页面显示正常，控制台无错误或警告。公网浏览器验收发生连接超时，尚未完成公网交互复测。
- 仓库已开启 Issues，后续可提交界面和功能调整需求。

## 自动验证

- 15 项测试通过，分布于领域规则、演示流程、PostgreSQL 权限/事务流程和异步数据刷新。
- PostgreSQL 测试实际执行表、RLS、函数及注册触发器，验证普通用户不能自提权、维护设备、审批或查看他人预约详细信息。
- 相邻预约允许，重叠拒绝；关闭设备、跨日、过去时间、非开放日、非半小时边界被拒绝。
- 考试只返回题目与选项，服务端评分；未完成答案被拒绝；合格凭证绑定邮箱、限时、一次性；伪造管理员 metadata 仍生成普通用户。
- 续约在批准时段内原子衔接同一使用者的原预约，记录交接，不伪造物理归还。
- 登出后旧账号的延迟刷新响应不会覆盖新会话。
- 演示与正式两种模式均通过 TypeScript 编译和 Vite 构建。
- 检查正式静态输出：不包含演示模块、默认演示账号、本地题库。
- 缺失正式配置或传入 Supabase secret key 会阻止构建。
- npm audit：0 已知漏洞（包含开发依赖，检查时点如上）。

## 浏览器验证

- 工作台正常显示统计、两间实验室入口和预约记录。
- 普通用户在设备详情提交预约，记录显示待审批。
- 管理员进入独立后台批准该申请，待审批数量减少，用户通知生成。
- 注册流程：填写信息、阅读条例、确认已读、随机十题、完整作答、合格后成功创建普通用户。
- 390 × 844 手机视口检查：页面无横向溢出；分类、房间、具体位置、项目筛选可见；账户头像和通知保留。已恢复默认桌面视口。

浏览器操作过程中环境有过连接超时，恢复后完成上述项目。页面演示数据包含一次本地验收申请及测试账号；这些数据只在当前浏览器，源代码种子数据没有混入验收账号。

## 尚需真实环境验证

- Supabase 云端迁移、邮件验证、实际图片上传、跨浏览器数据同步及并发负载。
- 课题组确认条例初稿；各设备管理员补充专用操作规程、图片和真实资产位置。
- 实体门禁权限由管理员人工同步，本版没有门禁硬件接口。

## 审查处理

独立只读审查提出的续约交接、续约初始时间和异步身份刷新问题均已修正。移除了可被匿名调用耗尽的全站考试请求总量上限，保留按邮箱的频率限制。当前考试入口无额外验证码服务；面向更大范围公开时，可按实际使用量增加服务端验证码和访问限速。设备停用保留历史，不提供删除资产与清空记录功能。
