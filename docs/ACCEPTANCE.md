# 第一版验收记录

日期：2026-09-13。第一版演示已发布，仓库现归属 [HCLab-gz/HCLab-Equipment-Management](https://github.com/HCLab-gz/HCLab-Equipment-Management)，网站入口为 [GitHub Pages 演示版](https://hclab-gz.github.io/HCLab-Equipment-Management/)。Supabase 项目尚未连接，当前不是多人共享预约系统。

## 仓库归属调整

- 从 YixFeng 转移至 HCLab-gz，接收方已接受转移。仓库 ID 仍为 `1368397092`，转移前后 main 提交均为 `b8b7180190f628ab4575bd969bd032bda3a92d80`，保留已有提交历史及 Issues 功能（转移时无未关闭 Issue）。
- 本地 origin、README、部署示例及未来账号服务的登录回调地址说明已更新至 HCLab-gz。
- GitHub Pages 使用 `https://hclab-gz.github.io/HCLab-Equipment-Management/`。旧 Pages 地址不应继续作为访问入口；历史验收结果见下文，新地址部署结果以仓库 Actions 为准。

## 发布验证

- [首次发布 Actions](https://github.com/HCLab-gz/HCLab-Equipment-Management/actions/runs/34759088080) 的安装、测试、构建与部署步骤全部成功。
- Pages 公网首页返回 HTTP 200，HTML 标题及项目子目录中的脚本、样式引用正确。
- 本地生产构建预览页面显示正常，控制台无错误或警告。公网浏览器验收发生连接超时，尚未完成公网交互复测。
- 仓库已开启 Issues，后续可提交界面和功能调整需求。

## 自动验证

- 15 项测试通过，分布于领域规则、演示流程、PostgreSQL 权限/事务流程和异步数据刷新。
- PostgreSQL 测试实际执行表、RLS、函数及注册触发器，验证普通用户不能自提权、维护设备、审批或查看他人预约详细信息。
- 相邻预约允许，重叠拒绝；关闭设备、跨日、过去时间、非开放日、非半小时边界被拒绝。
- 考试只返回题目与选项，服务端评分；未完成答案被拒绝；合格凭证绑定邮箱、限时、一次性；伪造管理员 metadata 仍生成普通用户。
- 续约在批准时段内原子衔接同一使用者的原预约，记录交接，不伪造物理归还。
- 登出后旧账号的延迟刷新响应不会覆盖新会话。
- 演示与正式两种模式均通过 TypeScript 编译和 Vite 构建。
- 检查正式静态输出：不包含演示模块、默认演示账号、本地题库。
- 缺失正式配置或传入 Supabase secret key 会阻止构建。
- npm audit：0 已知漏洞（包含开发依赖，检查时点如上）。

## 浏览器验证

- 工作台正常显示统计、两间实验室入口和预约记录。
- 普通用户在设备详情提交预约，记录显示待审批。
- 管理员进入独立后台批准该申请，待审批数量减少，用户通知生成。
- 注册流程：填写信息、阅读条例、确认已读、随机十题、完整作答、合格后成功创建普通用户。
- 390 × 844 手机视口检查：页面无横向溢出；分类、房间、具体位置、项目筛选可见；账户头像和通知保留。已恢复默认桌面视口。

浏览器操作过程中环境有过连接超时，恢复后完成上述项目。页面演示数据包含一次本地验收申请及测试账号；这些数据只在当前浏览器，源代码种子数据没有混入验收账号。

## 尚需真实环境验证

- Supabase 云端迁移、邮件验证、实际图片上传、跨浏览器数据同步及并发负载。
- 课题组确认条例初稿；各设备管理员补充专用操作规程、图片和真实资产位置。
- 实体门禁权限由管理员人工同步，本版没有门禁硬件接口。

## 审查处理

独立只读审查提出的续约交接、续约初始时间和异步身份刷新问题均已修正。移除了可被匿名调用耗尽的全站考试请求总量上限，保留按邮箱的频率限制。当前考试入口无额外验证码服务；面向更大范围公开时，可按实际使用量增加服务端验证码和访问限速。设备停用保留历史，不提供删除资产与清空记录功能。
